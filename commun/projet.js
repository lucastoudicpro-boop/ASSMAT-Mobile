/* Adresse du projet Supabase, partagée par les deux applications.
   À remplir une seule fois ici, avant de compiler.

   La clé « anon » est publique par conception : elle ne donne accès à rien
   par elle-même. Ce sont les règles de la base qui protègent les données.
   Laisser ces deux champs vides fait réapparaître la saisie manuelle dans les
   réglages, utile pour essayer un autre projet. */
const PROJET = {
  url: '',
  cle: ''
};
