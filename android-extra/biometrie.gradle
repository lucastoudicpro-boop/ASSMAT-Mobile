// Ajoute a la fin de android/app/build.gradle par le workflow.
// Gradle accepte plusieurs blocs dependencies : ils sont fusionnes.
dependencies {
    implementation "androidx.biometric:biometric:1.1.0"
}
